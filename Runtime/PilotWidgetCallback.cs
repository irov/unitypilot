namespace Pilot.SDK
{
    /// <summary>
    /// Callback for widget actions from the Pilot dashboard (button click, switch toggle, etc.).
    /// </summary>
    public delegate void PilotWidgetCallback(PilotAction action);
}
